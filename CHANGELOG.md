# 更新日志

## V2.2 - 2026-07-18

- 修复 IMAP 邮件只有 HTML 正文时列表只显示标题的问题，桌面端与 Android 端现在都会提取可读正文并用于验证码识别。
- 软件显示名称和 Windows 发布文件统一改为 `wrmail`，同时保留原有本地数据目录与 Android 包名，升级后继续读取已有数据。
- Windows 发布方式改为无 UPX 的目录版便携 ZIP，并生成 SHA-256 校验文件，移除仓库直接分发单文件自解压 EXE 的流程。

## V2.1 - 2026-07-16

- 修复手机号验证码接口连接失败时直接暴露底层异常的问题，增加重试与代理回退，并提供更清晰的错误提示。
- 优化手机号管理窗口：绑定邮箱选中背景保持连续，复制手机号改为显式按钮，并统一底部操作按钮尺寸。
- 重构账号库与取件区域布局，优化分类选择、账号卡片、空结果状态和窄窗口下的控件排列。
- 快速操作在较窄窗口中改为两排展示，避免“手机号管理”和“手机号取码”等文字被截断。
- 增加手机号取码与手机号管理回归测试，覆盖 API 数据解析、网络失败脱敏、选中状态和复制交互。

## V2.0 - 2026-07-14

- 修复 IMAP 协议选择无法在重启后保留的问题，并优化 OAuth 权限错误提示、连接重试、UID 读取和批量并发。
- 账号分类改为可持久化的动态菜单，支持新增、重命名和删除；删除分类时账号自动回到“未使用”。
- 分类下拉、批量移动、账号卡片、手机号管理、导入和导出统一使用当前分类配置。
- 优化 Windows 启动路径，网络、认证和手机号元数据依赖改为按需加载，首屏账号卡片延后构建。
- 默认构建改为快速启动目录版，同时保留 `-OneFile` 单文件构建选项。
- 重整桌面端布局与视觉层级，统一中性色和蓝色强调，精简账号操作并增加空结果引导。

## V1.9 - 2026-06-06

- 将账号操作入口移动到左侧邮箱列表上方，并独立成小卡片展示。
- 优化左侧邮箱列表、移动分类按钮、取件进度和结果区域的视觉层级。
- 调整进度状态文字样式，保持主界面标题和状态信息区分清晰。
- 修复手机号管理中绑定邮箱菜单会展示已绑定到其他手机号邮箱的问题。

## V1.8 - 2026-06-04

- 优化左侧邮箱分类移动按钮样式，让 `Plus`、`Free`、`封禁`、`未使用` 更贴合批量归类操作。
- 统一账号卡片内 `手机`、`邮箱`、`标签`、`复制` 小功能按钮样式。
- 保留已打标签按钮的绿色高亮状态，便于快速区分已标记账号。

## V1.7 - 2026-06-03

- 左侧账号卡片新增自定义标签功能，可为单个账号设置或清除标签。
- 标签内容随账号一起加密保存，并支持导出后重新导入恢复。
- 账号卡片会直接显示已设置的标签内容，未打标签和已打标签按钮使用不同颜色区分。
- 优化标签按钮尺寸，避免两字标签撑开账号列表布局。

## V1.6 - 2026-06-01

- 新增独立手机号取码窗口，支持批量导入手机号和短信 API 后单独取码。
- 独立手机号取码列表改为本地加密存储，更新或重启后自动保留。
- 手机号取码窗口新增复制手机号功能，自动识别国家和地区区号并只复制本地号码。
- 优化手机号取码窗口深色模式同步、取码进度展示和复制交互。
- 调整顶部开关位置，将导入后自动取件和简洁模式移到主窗口顶部。

## V1.5 - 2026-05-31

- 优化主界面布局，支持左右拖拽调整邮箱列表和取件区域宽度。
- 优化取件进度展示，新增进度卡片和百分比状态。
- 调整取件结果区域层级和分隔样式。
- 修复打包后标题栏小图标显示不正确的问题。

## V1.4 - 2026-05-30

- 优化手机号管理窗口布局，手机号列表显示更多内容，底部按钮避免文字压缩。
- 将邮箱绑定和单个解绑合并到已绑定邮箱后的小三角菜单。
- 右键手机号仅保留复制手机号功能，减少误操作。
- 优化主界面右侧工具栏窄窗口自适应，按钮会自动换行。
- 优化手机号绑定导入、导出、复制和验证码获取相关交互。

## V1.3 - 2026-05-30

- 新增手机号管理，支持手机号和邮箱绑定、手机号验证码获取、手机号导入导出。
- 邮箱导入导出支持保留关联手机号和短信 API，重新导入后自动恢复绑定关系。
- 左侧邮箱行新增单独取邮箱验证码、取手机验证码和复制邮箱按钮。
- 优化验证码识别，只显示和复制纯验证码，避免把 `code` 或无验证码提示误识别为验证码。
- 优化左侧列表宽度、按钮间距、状态提示和字体渲染。
- 手机号管理窗口新增复制手机号按钮。

## V1.2 - 2026-05-28

- 新增 Plus / Free / 已封禁分类导入、导出和批量标记流程。
- 导出账号按分类分组并加入可读分割线，重新导入时自动跳过分割线。
- 新增深色 / 浅色模式切换，并保存用户选择。
- 修复深色模式下结果区、导入窗口和邮件详情窗口出现白底的问题。
- 优化左侧邮箱列表密度、按钮等宽布局、应用图标显示和字体清晰度。

## V1.1 - 2026-05-28

- 重构为多模块工程结构，不再把所有逻辑堆在单个文件中。
- 主界面迁移到 PyQt6，并重做为现代化桌面布局。
- 优化左侧邮箱列表密度、勾选交互、取件结果展示密度和顶部控制区布局。
- 优化字体渲染、复选框可点击范围、批量取件时的稳定性和速度体验。
- 左侧邮箱列表支持“未使用 / Plus / Free / 已封禁”分类菜单，支持批量标记并按邮箱 A-Z 排序。
- 导出账号时带分类，重新导入导出文件会自动进入对应菜单。
- 重新打包单文件 exe。

## V1.0 - 2026-05-27

- 建立独立版本号体系。
- 软件标题和主界面显示当前版本。
- 新增 `VERSION` 文件。
- 新增 `bump_version.ps1`，支持 `V1.0 -> V1.1 -> ... -> V1.9 -> V2.0` 自动递增。
